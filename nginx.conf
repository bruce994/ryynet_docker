
#user  nobody;
worker_processes  8;

#error_log  logs/error.log;
#error_log  logs/error.log  notice;
#error_log  logs/error.log  info;

#pid        logs/nginx.pid;


events {
    worker_connections  20000;
    multi_accept on;
    use epoll;
}



http {
    include       mime.types;
    default_type  application/octet-stream;
    server_names_hash_bucket_size 256;

    #log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
    #                  '$status $body_bytes_sent "$http_referer" '
    #                  '"$http_user_agent" "$http_x_forwarded_for"';

    #access_log  logs/access.log  main;

    sendfile        on;
    #tcp_nopush     on;

    #keepalive_timeout  0;
    keepalive_timeout  65;

    #gzip  on;


log_format gzip '$remote_addr - $remote_user [$time_local] '
                '"$request" $status $bytes_sent '
                '"$http_referer" "$http_user_agent" "$gzip_ratio"';
open_log_file_cache max=1000 inactive=20s valid=1m min_uses=2;

limit_conn_zone $binary_remote_addr zone=limit:10m; #是针对每个IP定义一个存储session状态的容器。这个示例中定义了一个10m的容器，按照32bytes/session，可以处理320000个session
limit_conn_log_level info;

server {
	server_name _;
	listen       80  default_server;
	return       404;
}


#php分流
upstream bypass-php{
    ip_hash;
    server lanren-haibao:9000 weight=5;
    server virtural:9000 weight=4;
    #server 47.99.240.232:9001 weight=2;
    #server 47.99.240.232:9002 weight=2;
    #server 47.99.240.232:9003 weight=2;
}
#END

#php分流
upstream bypass-php-lanren{
    ip_hash;
    server lanren:9000 weight=5;
    server virtural:9000 weight=4;
    server test:9000 weight=4;
}
#END



include /usr/share/nginx/html/conf/*.conf;  
include /usr/share/nginx/html/www/shop.lanrenmb.com/*.conf;
include /usr/share/nginx/html2/20180516.tp.lanrenmb.com/*.conf;
include /usr/share/nginx/html2/jnxqhgy.top/*.conf;
include /usr/share/nginx/html2/20190228.tp.lanrenmb.cn/*.conf;
include /usr/share/nginx/html2/20190213.tp.lanrenmb.cn/*.conf;
include /usr/share/nginx/html2/20191111.tp.whlanren.com/*.conf;

}
